import functions from './functions.js';

idBox.addEventListener("click", functions.boxClickEvent);
idShowButton.addEventListener("click", functions.showList);
idAddBeforeButton.addEventListener("click", functions.addItemBefore);
idAddAfterButton.addEventListener("click", functions.addItemAfter);
idDeleteButton.addEventListener("click", functions.deleteItem);

